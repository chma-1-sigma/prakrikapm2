<Window x:Class="AgroControlLaboratory.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="АГРОКОНТРОЛЬ - Лаборатория"
        Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Background="#F0F0F0">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="АГРОКОНТРОЛЬ" 
                       FontSize="28" FontWeight="Bold" 
                       Foreground="#7B1FA2"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,5"/>

            <TextBlock Grid.Row="1" Text="Модуль лаборатории" 
                       FontSize="14" 
                       Foreground="#666"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <TextBlock Grid.Row="2" Text="Логин" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" Grid.Row="3" Height="35" Margin="0,0,0,15" Text="lab.vasilieva"/>

            <TextBlock Grid.Row="4" Text="Пароль" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Grid.Row="5" Height="35" Margin="0,0,0,20" Password="123"/>

            <TextBlock Grid.Row="6" Text="Введите код с картинки" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>

            <Grid Grid.Row="7" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image x:Name="imgCaptcha" Grid.Column="0" Width="260" Height="70" Stretch="Fill"/>
                <Button x:Name="btnRefresh" Grid.Column="1" Content="⟳" Width="50" Height="50" Margin="10,0,0,0" FontSize="20" Background="#7B1FA2" Foreground="White"/>
            </Grid>

            <TextBox x:Name="txtCaptcha" Grid.Row="8" Height="35" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnLogin" Content="Войти" Width="120" Height="40" Margin="0,0,15,0" Background="#7B1FA2" Foreground="White" FontWeight="Bold"/>
                <Button x:Name="btnCancel" Content="Отмена" Width="120" Height="40" Background="#E53935" Foreground="White" FontWeight="Bold"/>
            </StackPanel>

            <TextBlock x:Name="lblStatus" Grid.Row="10" Text="" 
                       Foreground="#E53935"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"/>
        </Grid>
    </Grid>
</Window>